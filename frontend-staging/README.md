# Staging storefront for https://new.wineknot.co.il
#
# Populated on the server by `scripts/deploy-staging.sh` from the redesign branch.
# Do not put a second copy of the site in git — apex stays `frontend/public`.
